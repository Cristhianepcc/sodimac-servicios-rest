Flask==3.0.3
# 2.0.51+ : las versiones anteriores no resuelven las anotaciones de
# union (X | None) de los modelos en Python 3.14 y la app no arranca
# con REPO_BACKEND=sqlalchemy.
SQLAlchemy>=2.0.51
psycopg[binary]>=3.2.10
python-dotenv==1.0.1
# Mensajeria asincrona con RabbitMQ (integracion con los procesos BPM)
pika==1.4.2
# Documentacion OpenAPI 3 / Swagger UI
apispec==6.10.0
apispec-webframeworks==1.2.0
